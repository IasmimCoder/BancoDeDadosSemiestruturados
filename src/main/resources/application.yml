spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cadastro_empresas_reguladas
    username: postgres
    password: 150150
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  h2:
    console:
      enabled: true

server:
  port: 8080
